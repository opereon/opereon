hostname: <% @key %>
domain: example.com
fqdn: <% hostname + '.' + domain %>
ssh_dest:
  hostname: 127.0.0.1
  port: 8820
  username: root
  auth:
    method: public-key
    identity_file: ../keys/vagrant

