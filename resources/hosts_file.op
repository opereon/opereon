# Update /etc/hosts file
proc hosts_file(hosts = $$hosts) {
    run on host in hosts {
        file_copy from: "./etc/hosts.ot" to: "/etc/hosts" process: true
        command "cat /root/hosts > /etc/hosts"
    }
}

